import styled from "styled-components"

export const HomeListWrapper = styled.div`
	.section {
		margin: 0 -8px;
		display: flex;
		flex-wrap: wrap;
		.loading {
			display: flex;
			justify-content: center;
			align-items: center;
		}
	}
`
